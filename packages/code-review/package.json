{
  "name": "@appverk/opencode-code-review",
  "version": "0.4.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean && bun scripts/copy-assets.mjs",
    "test": "bun run build && vitest run --config vitest.config.ts",
    "test:nobuild": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.14.19"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "tsup": "^8.5.0",
    "vitest": "^3.2.5",
    "@types/node": "^22.15.3"
  }
}
